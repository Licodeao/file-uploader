{
  "name": "file-uploader",
  "version": "1.0.3",
  "description": "Implement file upload by using「ReactJS + NestJS」",
  "scripts": {
    "release:changelog": "standard-version --infile changelog.md --same-file --skip.bump --skip.commit --skip.tag",
    "release:version": "bumpp package.json --execute=\"npm run release:changelog && git add changelog.md\" --commit \"build: publish v%s\"  --tag --all",
    "release:publish": "clean-publish",
    "release": "npm run release:version"
  },
  "keywords": [
    "ReactJS",
    "NestJS",
    "FileUpload"
  ],
  "author": "Licodeao <https://github.com/Licodeao>",
  "license": "MIT",
  "devDependencies": {
    "bumpp": "^9.3.0"
  },
  "dependencies": {
    "clean-publish": "^4.2.0"
  }
}
